//对给定的字符串，本题要求你输出最长对称子串的长度。
例如，给定Is PAT&TAP symmetric?，最长对称子串为s PAT&TAP s，于是你应该输出11。


#include<stdio.h>
#include<string.h>
#define MAX 10001
int dp[MAX][MAX];
int max(int a, int b){
    return (a>b)? a: b;
}
int main(){
    char s[10001];
    gets(s);
    int max1=0;
    int len=strlen(s);
    for(int i=1; i<=len; i++){
        for(int j=1; j<=len; j++){
            if(s[i-1]==s[len-j]){dp[i][j] = dp[i-1][j-1]+1;max1=max(dp[i][j], max1);}
            else dp[i][j] = 0;
        }
    }
    printf("%d\n", max1);
    return 0;
}
